{
  "compilerOptions": {
    "outDir": "./build",
    "allowJs": true,
    "moduleResolution": "node",
    "module": "es6",
    "target": "es5"
  },
  "include": ["./src/**/*"],
  // Exclude the examples, since they intentionally
  // have errors to highlight dev tooling abilities.
  "exclude": ["./src/examples"]
}
